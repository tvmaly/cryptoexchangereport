package utility

import "strings"

// NormalizeSymbol a placeholder that will let us adapt the system to
// some standard symbology format. wrap all raw exchange symbol values used with a call to this
func NormalizeSymbol(symbol string) string {
	return symbol
}
